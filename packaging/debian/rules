#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_install:
	cd daemon && pip3 install --prefix=$(CURDIR)/debian/gnome-activities/usr --no-deps .
	install -D -m 644 daemon/gnome-activities.service \
		$(CURDIR)/debian/gnome-activities/usr/lib/systemd/user/gnome-activities.service
